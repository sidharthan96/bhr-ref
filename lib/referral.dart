import 'package:bhrref/main.dart';
import 'package:flutter/material.dart';

class ReferralPathway {
  String specialty;
  String referralMethod;
  String url;
  String email;

  ReferralPathway(this.specialty, this.referralMethod, this.email, this.url);

  }

